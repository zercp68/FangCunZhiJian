using SerializeReferenceEditor;
using System.Collections.Generic;
using UnityEngine;


public class TestSystem : MonoBehaviour
{
    //    [Header("References")]
    //    [SerializeField] private HorizontalCardHolder handHolder;
    //    [SerializeField] private List<CardData> deckData;
    //    private void Start()
    //    {
    //        CardSystem.Instance.Setup(deckData);
    //    }

    // 先赋值你的武器卡牌配置
    //public WeaponCardData testWeaponData;

    //void Start()
    //{
    //    // 直接生成一张武器卡牌
    //    weaponHolder.SpawnWeaponCard(testWeaponData);
    //}







    #region 第一次测试
    //[SerializeField]private List<Card> cards;   // 在 Inspector 中拖入多种卡牌数据

    //private void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.Space))
    //    {
    //        if (cards == null || cards.Count == 0)
    //        {
    //            Debug.LogWarning("请在 TestSystem 的 Inspector 中填入 testCards");
    //            return;
    //        }

    //        // 随机选择一张卡牌数据
    //        Card randomcard = cards[Random.Range(0, cards.Count)];
    //        handHolder.AddCard(randomcard);
    //    }
    //}
    #endregion
}